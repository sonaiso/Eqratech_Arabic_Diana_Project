# SFGCOQ - نظام التحقق الرسمي للنحو العربي
## Systemic Functional Grammar - COQ Formal Verification

SFGCOQ هو مشروع للتحقق الرسمي من القواعد النحوية العربية باستخدام نظرية النحو الوظيفي النظامي (Systemic Functional Grammar) ونظام COQ للإثبات الرياضي.

### 🎯 الأهداف

1. **التحقق الرسمي**: إثبات صحة القواعد النحوية العربية رياضياً
2. **التحليل النحوي**: تحليل الجمل العربية بناءً على النظرية الوظيفية
3. **قاعدة بيانات شاملة**: توفير مكتبة واسعة من القواعد والأمثلة
4. **واجهة برمجية**: API متكاملة للتكامل مع التطبيقات الأخرى

### 📁 هيكل المشروع

```
arabic-formal-verification/
├── coq/                      # ملفات COQ للتحقق الرسمي
│   ├── ArabicGrammar.v       # التعريفات الأساسية والنظريات
│   ├── NawasighRules.v       # قواعد كان وأخواتها وإنّ وأخواتها
│   ├── MorphologyRules.v     # قواعد الصرف (المثنى والجموع)
│   └── Makefile              # أتمتة البناء
├── docs/
│   └── web/
│       ├── index.html        # الواجهة الرئيسية
│       ├── style.css         # الأنماط البصرية
│       ├── app.js            # منطق التطبيق
│       └── data.json         # بيانات القواعد النحوية
├── install.sh                # سكربت التثبيت
└── README.md                 # هذا الملف
```

### 🚀 التثبيت والتشغيل

#### التثبيت السريع

```bash
cd arabic-formal-verification
./install.sh
```

#### تثبيت COQ يدوياً

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install coq
```

**macOS:**
```bash
brew install coq
```

**Windows:**
- قم بتحميل [COQ Platform](https://github.com/coq/platform/releases)
- أو استخدم `choco install coq`

#### تجميع الإثباتات

```bash
cd arabic-formal-verification/coq
make          # تجميع جميع الملفات
make verify   # التحقق من الإثباتات
make clean    # حذف الملفات المجمعة
```

#### تشغيل الواجهة المحلية

```bash
cd arabic-formal-verification/docs/web
python -m http.server 8080
```

ثم افتح المتصفح على: `http://localhost:8080`

#### أو باستخدام Node.js

```bash
npx http-server arabic-formal-verification/docs/web -p 8080
```

### 📚 المحتويات

#### القواعد النحوية المدعومة

- **الجملة الاسمية**: المبتدأ والخبر، كان وأخواتها، إن وأخواتها
- **الجملة الفعلية**: الفعل والفاعل، المفعول به، المبني للمجهول
- **الحروف**: حروف الجر، حروف العطف
- **الصرف**: المثنى، جمع المذكر السالم، جمع المؤنث السالم

#### الوظائف الثلاث (Metafunctions)

1. **الوظيفة الفكرية (Ideational)**: تمثيل الخبرات والأفكار
2. **الوظيفة البينشخصية (Interpersonal)**: العلاقات الاجتماعية
3. **الوظيفة النصية (Textual)**: تنظيم الرسالة

### 🔗 واجهة البرمجة (API)

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/grammar` | استرجاع قائمة القواعد |
| GET | `/api/grammar/{id}` | استرجاع قاعدة محددة |
| POST | `/api/verify` | التحقق من جملة |
| GET | `/api/parse/{sentence}` | تحليل جملة |
| GET | `/api/metafunctions` | معلومات الوظائف |

### 📖 النحو الوظيفي النظامي

النحو الوظيفي النظامي (SFG) هو نظرية لغوية طورها مايكل هاليداي، تنظر إلى اللغة كنظام اجتماعي-دلالي. تركز على:

- **الاختيار**: اللغة كنظام من الاختيارات
- **الوظيفة**: كيف تؤدي اللغة وظائف مختلفة
- **السياق**: العلاقة بين اللغة والسياق الاجتماعي

### 🔬 التحقق الرسمي بـ COQ

COQ هو نظام مساعد للإثبات الرياضي. نستخدمه لـ:

- إثبات صحة القواعد النحوية
- ضمان عدم وجود تناقضات
- توثيق القواعد بشكل رياضي دقيق

#### ملفات COQ المتوفرة

| الملف | المحتوى |
|-------|---------|
| `ArabicGrammar.v` | التعريفات الأساسية: الإعراب، الجملة الاسمية، الجملة الفعلية |
| `NawasighRules.v` | قواعد النواسخ: كان وأخواتها، إنّ وأخواتها |
| `MorphologyRules.v` | قواعد الصرف: المثنى، جمع المذكر السالم، جمع المؤنث السالم |

#### مثال على إثبات COQ

```coq
Theorem nominal_sentence : 
  forall s p, is_mubtada s -> is_khabar p -> valid_sentence (Nominal s p).
```

#### مثال كامل من ArabicGrammar.v

```coq
(* مثال: "الكتابُ جديدٌ" - The book is new *)
Example kitab_jadid :
  let mubtada := mkNoun "كتاب" Nominative Masculine Singular true in
  let khabar := mkNoun "جديد" Nominative Masculine Singular false in
  valid_nominal_sentence (mkNominal mubtada khabar).
Proof.
  unfold valid_nominal_sentence.
  unfold is_valid_mubtada, is_valid_khabar.
  simpl.
  split; reflexivity.
Qed.
```

### 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. Fork المستودع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

### 📄 الرخصة

هذا المشروع جزء من مشروع Eqratech Arabic Diana.

### 📞 التواصل

للأسئلة والاستفسارات، يرجى فتح Issue في المستودع.
